from fastapi import FastAPI

app = FastAPI(title="{{ spec.display_name }}", description="{{ spec.description }}")


@app.get("/health")
def health():
    return {"status": "ok", "service": "{{ spec.name }}"}
